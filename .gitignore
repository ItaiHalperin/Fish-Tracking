.DS_Store
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Raw data and regenerable outputs — never part of the submission
videos/
dataset/
dataset.yaml
crops/
labels/
labels_*/
annotation_samples/
seed_queue.txt
out/
results/
results_pipeline/
results_comparison/
*.zip

# Upstream pretrained weights (re-downloaded by ultralytics on demand).
# Our own trained weights ARE tracked — see archive/README.md.
/*.pt
